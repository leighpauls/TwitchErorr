TwitchErorr is a webapp for testing proxies that acts as a simple server which returns HTTP error codes on request.

The project was originally implemented as a very simple App Engine Python, but is being re-written as a node.js project.